mouseOver = true;

// ignore mouseOver events on Instances layer when inventory is open
// "solved" this by moving inventory up top so it doesn't matter as much


//with(oCharacterPool) {
//	if (visible && other.layer = layer_get_id("Instances" )) {
//		other.mouseOver = false;
//	}
//}
